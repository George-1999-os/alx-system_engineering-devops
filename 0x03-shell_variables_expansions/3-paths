#!/bin/bash
# 3-paths: count the number of directories in $PATH

count=0
IFS=':' read -r -a dirs <<< "$PATH"

for dir in "${dirs[@]}"; do
    if [ -n "$dir" ]; then
        ((count++))
    fi
done

echo "$count"
